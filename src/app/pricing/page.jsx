import PricingIndex from "@/components/pricing";
import React from "react";

export const metadata = {
   title: "Cosmicforge Healthnet | Our Pricing",
};

const Pricing = () => {
   return <PricingIndex />;
};

export default Pricing;
